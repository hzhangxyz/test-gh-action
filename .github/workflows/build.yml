name: build

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
    branches:
      - '*'
  pull_request:

jobs:

  hello:
    name: echo hello
    runs-on: ubuntu-latest

    steps:
      - name: echo hello
        run: echo hello
